services:
  - type: web
    name: sales-forecasting
    runtime: python
    buildCommand: pip install -r requirements.txt && python -c "from prophet import Prophet; Prophet()"
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0